{
  "name": "cognitive-operating-system",
  "private": true,
  "version": "0.1.0",
  "description": "COS — truth-grounded operator dashboard (V1 scaffold)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "web:dev": "npm run dev -w web",
    "web:build": "npm run build -w web",
    "web:lint": "npm run lint -w web"
  },
  "engines": {
    "node": ">=18"
  }
}
