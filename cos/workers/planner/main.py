"""
Planner worker — idea → structured plan JSON, task tree, GitHub sync hooks.

Run as a queue consumer (wired in Phase 1).
"""

def main() -> None:
    print("cos-planner-worker: stub — connect orchestration in Phase 1")


if __name__ == "__main__":
    main()
